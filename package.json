{
  "name": "trts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "start_showcase": "parcel src/index.html --host 0.0.0.0",
    "prebuild": "rm -rf dist",
    "build": "parcel build src/index.html --public-url '.'"
  },
  "devDependencies": {
    "@types/node": "^13.1.6",
    "parcel": "^1.10.3",
    "typescript": "^3.7.5"
  },
  "license": "MIT",
  "dependencies": {
    "noisejs": "^2.1.0",
    "phaser": "^3.22.0",
    "phaser-plugin-debug-draw": "^4.3.0"
  }
}
